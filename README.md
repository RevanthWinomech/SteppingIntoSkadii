# SteppingIntoSkadii
## Project scope:
The objective of this project is to start your C# journey with some basic tools in a standard way.
### To Do:
 - Create a DB with a table with few columns you prefer.
 - In the C# service, there should be two endpoints exposed. One for POST and another for GET.
 - POST endpoint should get the input from the user and insert the data into DB.
 - GET should retrieve the latest values and send back in a specified model (you create)

### DB should be isolated and work on docker environment.
 - which means you should create the docker images for the db and create a init.db  file
 - I should be able to check your code against the docker file specified for the db
 - Should be able to test in postman (POST + GET) 
 -  test
